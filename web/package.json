{
  "name": "gideon-web",
  "private": true,
  "description": "gideon reading-progress web dashboard + Playwright regression tests",
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
